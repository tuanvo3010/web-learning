// //Logical operators;
// // || OR; // Tìm giá trị đúng đầu tiên
// // && And; // Tìm giá trị sai đầu tiên

// // Cấu trúc rẽ nhánh
// // if else
// // switch case

// // Cấu trúc lặp
// // for, for in, for of
// // while
// // do while

// // Xử lí lỗi
// // try catch

// // Đạng đơn giản nhất
// // Chỉ thực hiện đoạn mã nếu condition là true
// // Nếu không thì không làm gì khác
// if (condition) {
// }

// // Thực hiện đoạn mã trong if nếu condition là true
// // Nếu không thì thực hiện đoạn mã trong else
// if (condition) {
// } else {
// }

// // Thực hiện đoạn ãm trong if nếu condition là true
// // Nếu không thì thực hiện đoạn mã trong else ì nếu otherCondition
// // Nếu không thì thực hiện đoạn mã trong else
// if (condition) {
// } else if (otherCondition) {
// } else {
// }

// // Switch case
// // ? - Thay thế if else trong trường hợp muốn gán giá trị theo điều kiện
// // if else cho phép kiểm tra điều kiện trong 1 khoảng giá trị
// // switch case kiếm tra theo 1 giá trị cụ thể
// switch (value) {
//     case case1:
//         // code
//         break; // thay thế bằng return
//     case case2:
//         //code
//         break;
//     default;
//         //code
// }

// let var = condition ? value1 : value2;
// function calcCommissions(totalSales) {
//   let commission =
//     totalSales <= 100
//       ? 0.05 * totalSales
//       : totalSales <= 300
//       ? 0.1 * totalSales
//       : 0.2 * totalSales;
//   return commission;
// }
// console.log(calcCommissions(242));
